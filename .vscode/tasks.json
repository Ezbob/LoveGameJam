{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run it",
            "type": "shell",
            "command": "lovec ${workspaceFolder}",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "test it",
            "type": "shell",
            "command": "lua53 ${workspaceFolder}/test.lua"
        }
    ]
}